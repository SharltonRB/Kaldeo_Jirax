# Production Environment Variables Example
# Copy this file to .env.prod and fill in the actual values
# NEVER commit .env.prod to Git!

# ================================
# DATABASE CONFIGURATION
# ================================
POSTGRES_DB=issue_tracker
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_postgres_password_here
POSTGRES_PORT=5432

# Database Pool Configuration
DB_POOL_SIZE=20
DB_POOL_MIN_IDLE=5

# ================================
# REDIS CONFIGURATION
# ================================
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=your_secure_redis_password_here

# ================================
# JWT CONFIGURATION (CRITICAL - Generate secure values)
# ================================
# Generate a strong secret: openssl rand -base64 64
JWT_SECRET=your_very_long_and_secure_jwt_secret_key_at_least_256_bits_long
JWT_EXPIRATION=86400000
JWT_REFRESH_EXPIRATION=604800000

# ================================
# APPLICATION CONFIGURATION
# ================================
SPRING_PROFILES_ACTIVE=prod
LOG_LEVEL=INFO
REQUEST_LOG_LEVEL=WARN

# ================================
# CORS CONFIGURATION
# ================================
# Comma-separated list of allowed origins
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ================================
# RATE LIMITING
# ================================
AUTH_RATE_LIMIT=10
API_RATE_LIMIT=100

# ================================
# MONITORING
# ================================
PROMETHEUS_ENABLED=true

# ================================
# PORT CONFIGURATION
# ================================
BACKEND_PORT=8080
FRONTEND_PORT=80
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# ================================
# FRONTEND CONFIGURATION
# ================================
VITE_API_BASE_URL=https://yourdomain.com/api
VITE_WS_URL=wss://yourdomain.com/ws

# ================================
# DOCKER RESOURCE LIMITS (optional)
# ================================
POSTGRES_MEMORY_LIMIT=1g
POSTGRES_CPU_LIMIT=0.5
REDIS_MEMORY_LIMIT=512m
REDIS_CPU_LIMIT=0.25
BACKEND_MEMORY_LIMIT=2g
BACKEND_CPU_LIMIT=1.0
FRONTEND_MEMORY_LIMIT=256m
FRONTEND_CPU_LIMIT=0.25

# ================================
# DEPLOYMENT NOTES
# ================================
# 1. Copy this file: cp .env.prod.example .env.prod
# 2. Fill in all the values with your actual production credentials
# 3. NEVER commit .env.prod to Git
# 4. Use strong, unique passwords for each service
# 5. Generate JWT_SECRET with: openssl rand -base64 64
# 6. Update CORS_ALLOWED_ORIGINS with your actual domain(s)
# 7. For production, consider using Docker Secrets or a secret management service
