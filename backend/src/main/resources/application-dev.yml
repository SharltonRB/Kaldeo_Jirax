spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/issue_tracker_dev
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false
    defer-datasource-initialization: false
  
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
  
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0

logging:
  level:
    com.issuetracker: INFO
    org.springframework.web: INFO

# Development-specific JWT secret - ONLY FOR DEVELOPMENT
# NEVER use this in production - always use environment variables
jwt:
  secret: ${JWT_SECRET:ZGV2LXNlY3JldC1rZXktQ0hBTkdFLUlOLVBST0RVQ1RJT04tMTIzNDU=}