# Production Environment Variables Template
# Copy this file to .env.prod and fill in the actual values

# Database Configuration
POSTGRES_DB=issue_tracker
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_postgres_password_here
POSTGRES_PORT=5432
DB_POOL_SIZE=20
DB_POOL_MIN_IDLE=5

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=your_secure_redis_password_here
REDIS_SSL=false

# JWT Configuration (CRITICAL - Generate secure values)
JWT_SECRET=your_very_long_and_secure_jwt_secret_key_at_least_256_bits_long
JWT_EXPIRATION=86400000
JWT_REFRESH_EXPIRATION=604800000

# Application Configuration
SPRING_PROFILES_ACTIVE=prod
LOG_LEVEL=INFO
REQUEST_LOG_LEVEL=WARN

# CORS Configuration
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Rate Limiting
AUTH_RATE_LIMIT=10
API_RATE_LIMIT=100

# SSL Configuration (if using embedded SSL)
SSL_ENABLED=false
SSL_KEYSTORE_PATH=/app/ssl/keystore.p12
SSL_KEYSTORE_PASSWORD=your_keystore_password
SSL_KEYSTORE_TYPE=PKCS12
SSL_KEY_ALIAS=issue-tracker

# Security
REQUIRE_SSL=true
COOKIE_SECURE=true

# Monitoring
PROMETHEUS_ENABLED=true

# Logging
LOG_FILE_PATH=logs/application.log
LOG_MAX_SIZE=50MB
LOG_MAX_HISTORY=30
LOG_TOTAL_SIZE=1GB

# Port Configuration
BACKEND_PORT=8080
FRONTEND_PORT=80
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# Frontend Configuration
VITE_API_BASE_URL=https://yourdomain.com/api
VITE_WS_URL=wss://yourdomain.com/ws

# Docker Resource Limits (optional)
POSTGRES_MEMORY_LIMIT=1g
POSTGRES_CPU_LIMIT=0.5
REDIS_MEMORY_LIMIT=512m
REDIS_CPU_LIMIT=0.25
BACKEND_MEMORY_LIMIT=2g
BACKEND_CPU_LIMIT=1.0
FRONTEND_MEMORY_LIMIT=256m
FRONTEND_CPU_LIMIT=0.25